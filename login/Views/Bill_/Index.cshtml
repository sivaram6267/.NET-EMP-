@model IEnumerable<login.Models.Bill>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<h1 style="color:blue;">Bill_Details</h1>
<html>
<head>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>
</head>
<table class="table">
    
    <tr>
 
        <th>
            @Html.DisplayNameFor(model => model.Cubical_cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Food_cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Transport_cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee_details.Emp_Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
<tr>
    
    <td>
        @Html.DisplayFor(modelItem => item.Cubical_cost)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Food_cost)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Transport_cost)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Employee_details.Emp_Name)
    </td>
    @*<td>
        @Html.ActionLink("Edit", "Edit", new { id = item.Billing_Id }) |
        @Html.ActionLink("Details", "Details", new { id = item.Billing_Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Billing_Id })
    </td>*@
</tr>
    }

</table>
</html>
